language: cpp
compiler:
    - gcc-4.8
    - clang
install:
    - sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test
    - sudo apt-get -qq update
    - sudo apt-get -qq install g++-4.8
script:
    - $CXX --version
    - cd ./test && make clean && make && ./a.out
